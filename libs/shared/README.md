# @stockholm-transit/shared

## Building

Run `npx nx build @stockholm-transit/shared` to build the library.

## Running unit tests

Run `npx nx test @stockholm-transit/shared` to execute the unit tests via [Jest](https://jestjs.io).
